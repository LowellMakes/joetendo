include common

[main]

# ZeroRanger has been configured, for now, to use the MAME keys,
# so it doesn't need a custom keymap.

# ... In truth, I probably SHOULD make a keymap for it anyway,
# so that we rely on game-specific configuration less...

exit = command(pkill -f -15 vent)
